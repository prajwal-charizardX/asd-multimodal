#1 pip install flask

#2 pip install -r requirements.txt

If any error is caused in tf, opencv or nilearn while installing requirements.txt
#3 pip installl tensorflow
#4 pip install nilearn
#5 pip install opencv-python
#6 pip install opencv-python-headless

Finally, to run

#7 python app.py

Once it opens in browser, navigate to "/asd-multimodal/static/templates/script_gene.js", there, in line 110, replace fetch url with the url of #7

End the runtime in terminal, and restart the app

#8 python app.py


Everything should be good to go!!!!!!
Visit "https://autify.b12sites.com/index#home" for my webpage